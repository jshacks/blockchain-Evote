{
  "name": "client-web",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "build": "meteor build .build"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "meteor-node-stubs": "~0.2.4"
  }
}
